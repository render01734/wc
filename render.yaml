services:
  - type: web
    name: minecraft-server
    runtime: docker
    privileged: true
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: "5000"
      - key: MC_RAM
        value: "2G"
    healthCheckPath: /
    numInstances: 1
